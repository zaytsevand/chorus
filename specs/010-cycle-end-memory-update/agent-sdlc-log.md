# Agent-SDLC ledger — feature 010 (cycle-end memory update phase)

Audit trail for the chorus SDLC gate(s) run on this feature. A reviewer must be able to
reconstruct the run from this file alone (`SDLC-LAYER.md` § The ledger).

## Run context

- **Date**: 2026-06-12
- **Mode**: agent-SDLC lifecycle, **Gate C (implementation review)** only.
- **Why only Gate C**: the feature was implemented **directly into chorus canon** (Markdown
  skill/prompt-orchestration — there is no runtime, no plan.md/tasks.md). Gates A (design-of-plan)
  and B (plan/tasks) had no artefact to review and were not run. The corpus is the 010 changeset:
  `spec.md`, `quickstart.md`, the new `§ Memory update phase (sign-off)` in `SDLC-LAYER.md`, and the
  write-side-bookend cross-ref in `EXPLORATORY-PHASE.md`.
- **Corpus location**: worktree `/home/az/code/chorus-review/.claude/worktrees/pr-18/` (PR #18,
  branch `claude/open-issues-fw4tfw`). All briefs pinned the absolute worktree path.
- **Verdict**: **CLEARED** after one self-heal incorporation cycle. 3 gating 🔴 resolved & verified.

### Honest deviations (recorded, not hidden)

- **S5 / S1 (incorporation via speckit, orchestrator authors nothing)** — **deviated, operator-directed.**
  The artefact is canon prose authored directly at the operator's instruction ("remedy yellows then
  implement"), not generated by a speckit phase-runner. Incorporation of the 🔴 was therefore direct
  spec/canon editing, not `/speckit-clarify → plan`. The **vote mechanics that make a gate honest
  (S3/S6/S8/S9, author-excluded real votes, deterministic tally) were followed in full** — only the
  artefact-authorship route differs from the standard pipeline. Flagged so the deviation is legible.
- **Exploratory phase (Phase 0.7) compressed** — folded into the author briefs (each lens pointed at
  exact worktree paths + the 004 canon to verify reuse claims) rather than run as a standalone phase.
  Rationale: the corpus is 4 focused files; the exploratory phase earns its cost on large unfamiliar
  corpora. Recorded as a proportionate compression.

## Phase 0.5 — RSVP (roster of 9; language lens omitted — no code in scope)

| Lens | RSVP | Applicability (cited delta) | Expected stakes |
|------|------|------------------------------|-----------------|
| Security-and-Trust | **JOIN** | FR-007 secret pre-filter; memory-write trust boundary | 🔴-potential |
| Goldratt | **JOIN** | TOC-3 deferral resolution (its own held finding) | 🟡-potential |
| Evans | **JOIN** | DDD-2 ubiquitous-language at the write boundary (its own) | 🟡-potential |
| Richards | **JOIN** | "reuses 004's contract, no new write path" claim | 🟡-potential |
| Cooper | **JOIN** | accept/reject/defer operator surface; owns prior COOP-3 | 🟡-potential |
| Norman | **JOIN** | operator mental-model of memory writes; deferred-queue legibility | 🟡-potential |
| Uncle Bob | **JOIN** | SRP/DRY at the cite-not-restate seam | 🟡-potential |
| Beck | **JOIN** | FR-003a durable-predicate simplicity; quickstart-as-test | 🟡-potential |
| Delivery-and-Ops | **ABSTAIN** | only candidate was the ledger audit-record; no runtime/ops/cost surface | 🟢 |

J = 8 joiners.

### Seating decision — DecisionRecord `gateC-seating-1` (🟡, tie at cap)

- **point**: RSVP seating, J=8 > cap 5 (`DECISION-PRIMITIVE.md` catalog row 2).
- **band**: 🟡 — non-strict order at the 5th seat boundary (genuine tie).
- **sensor**: sort by (applicability quality/count, then expected stakes), + mandate guardrail.
- **chosen (seated panel)**: Security (only 🔴-potential, scope override), Goldratt (never out-seated
  on a new buildout — SDLC-LAYER mandate guardrail; owns TOC-3), Evans (owns DDD-2), Richards
  (load-bearing reuse claim; subsumes Uncle Bob's SRP-at-seam mandate), **Cooper** (owns prior COOP-3
  = strongest applicability continuity; operator-benefit mandate uncovered by seats 1–4).
- **alternatives (runner-ups, queued for async override)**: Norman (HCD legibility), Beck (FR-003a
  decidability), Uncle Bob (SRP — covered by Richards).
- **resolution**: default-applied, proceed (no operator interruption).
- **override**: operator may swap any runner-up for a seated lens and re-run the gate; cost ≈ one
  author+vote cycle for the swapped lens.

## Stage 2 — Findings register (uncapped author; I8 evidence gate passed — every finding file:line or [principle])

| ID | Lens | Proposed | Summary |
|----|------|:--:|---------|
| SEC-1 | Security | 🟡 | "Mechanical" secret pre-filter overclaims — no runtime ⇒ it's agent self-attestation; name it honestly. |
| SEC-2 | Security | 🟡 | Detector names only credential-shaped secrets; constitution's boundary includes structured "private project facts." |
| SEC-3 | Security | 🟢 | Boundary is genuinely the pre-filter, not the operator-confirm; SC-005 tests operator-independence. (clean) |
| SEC-4 | Security | 🟡 | `lens-specific` auto-write is the unwatched path; ledger must log its drops too. |
| SEC-5 | Security | 🟢 | "Memory is an index" preserved on the write side; harvest-to-replay surface closed. (clean) |
| SEC-6 | Security | 🟡 | `.gitignore` doesn't cover the agent-memory dir (belt-and-suspenders). |
| TOC-1 | Goldratt | 🟢 | Per-lens dispatch is the cheapest *legal* loop-closer; the cheap pass is S1/S9-illegal. (clean) |
| TOC-2 | Goldratt | 🟡 | Build-now cost-of-delay is a rationalization: 004's read-side never exercised; run a one-cycle manual experiment first. |
| TOC-3 | Goldratt | 🟡 | Phase plausibly but not *validated* on the binding constraint; re-learning cost asserted, not measured. |
| DDD-1 | Evans | 🟢 | Two-tier aggregate honored; no cache authors the addendum. (clean) |
| DDD-2 | Evans | 🟡 | "Deferred write-back" misnames an unconfirmed proposal; rename to "deferred proposal." |
| DDD-3 | Evans | 🟢 | "Delta" fenced as informal shorthand cleanly. (clean; minor two-senses nit) |
| DDD-4 | Evans | 🟢 | Deferral named/queued — the happening made honest. (clean) |
| ARCH-1 | Richards | 🟢 | Reuse of 004's write path is real, not forked. (clean) |
| ARCH-2 | Richards | 🟢 | Single-source-of-truth honored; reference-not-restate. (clean) |
| ARCH-3 | Richards | 🟡 | FR-010 reuse could silently fork later; add a doc-test fitness function. |
| ARCH-4 | Richards | 🟢 | Secret pre-filter correctly NOT claimed as reuse; additive upstream control. (clean) |
| ARCH-5 | Richards | 🟢 | Bookend integration sound; addendum-as-shared-resource bounded. (clean) |
| COOP-1 | Cooper | 🟢 | Reject/no-response split resolves prior COOP-3 well. (clean) |
| COOP-2 | Cooper | 🟡 | Unbounded "defer + re-offer every sign-off" is a nagging queue serving the system; bound it. |
| COOP-3 | Cooper | 🟡 | Accept/reject at sign-off invites rubber-stamp; re-present the diff outside sign-off. |
| COOP-4 | Cooper | 🟢 | `lens-specific` auto-write correctly not taxed to the operator. (clean) |
| COOP-5 | Cooper | 🟢 | The phase has a real, honestly-named operator goal. (clean) |

## Stage 3–4 — Vote tally (non-author votes; `net = P − O`; ≥+2 escalate, ≤−2 demote)

| Finding | P | C | O | net | Post-tally |
|---------|:-:|:-:|:-:|:---:|-----------|
| SEC-1 | 4 | 0 | 0 | +4 | 🟡 → **🔴 GATING** |
| SEC-2 | 2 | 2 | 0 | +2 | 🟡 → **🔴 GATING** |
| COOP-2 | 2 | 2 | 0 | +2 | 🟡 → **🔴 GATING** |
| SEC-4 | 1 | 3 | 0 | +1 | hold 🟡 |
| TOC-2 | 1 | 3 | 0 | +1 | hold 🟡 |
| TOC-3 | 0 | 3 | 1 | −1 | hold 🟡 |
| ARCH-3 | 1 | 2 | 1 | 0 | hold 🟡 |
| SEC-6 | 0 | 2 | 2 | −2 | demote → 🟢 |
| DDD-2 | 0 | 2 | 2 | −2 | demote → 🟢 |
| COOP-3 | 0 | 2 | 2 | −2 | demote → 🟢 |

Clean bills (SEC-3/5, TOC-1, DDD-1/3/4, ARCH-1/2/4/5, COOP-1/4/5): CONFIRM, hold 🟢.

## 🔴 resolution log — DecisionRecord `gateC-selfheal-1` (🟡 self-heal, cycle 1 of 3)

`resolution: auto-incorporated + verified`. Incorporation was direct canon editing (see deviation note).

| 🔴 | Fix incorporated | Where |
|----|-------------------|-------|
| SEC-1 | "mechanical" → "**agent-applied, ledger-audited**"; substrate-honesty clause (no runtime ⇒ persona-applied discipline verified by the ledger drop-record); spec MUST NOT call it "mechanical" | spec FR-007, Key Entities, Edge Cases; SDLC-LAYER § pre-filter bullet; quickstart ex.4 + checklist |
| SEC-2 | Detector class made **two-part**: credential-shaped **and** structured private project facts (hostnames, names, ticket IDs), citing the constitution boundary | spec FR-007, SC-005; SDLC-LAYER; quickstart ex.4 |
| COOP-2 | Re-offer **bounded to N=3** then **lapse** to a passively-readable terminal state (new "Lapsed proposal" entity; SC-003b) | spec FR-006, Key Entities, SC-003b; SDLC-LAYER § project-wide routing; quickstart ex.3 + checklist |
| +SEC-4 (held 🟡, folded) | Ledger drop-record logs drops on the auto `lens-specific` path too | spec FR-007, SC-005 |

### Verifying sensor — focused re-vote (3 non-author verifiers)

| 🔴 | Goldratt | Evans | Richards | Result |
|----|----------|-------|----------|--------|
| SEC-1 | NOT-CLEARED (stray "mechanical" at Edge Cases) | CLEARED (+ same non-gating note) | CLEARED | residual fixed → **CLEARED** |
| SEC-2 | CLEARED | CLEARED | CLEARED | **CLEARED** |
| COOP-2 | CLEARED (real terminal state, not cosmetic) | CLEARED (aggregate invariant satisfied) | CLEARED | **CLEARED** |

Richards also confirmed the fixes are **additive to 010** and did not fork 004's contract or break
the cite-not-restate seam (ARCH-1/ARCH-2 hold). Goldratt's NOT-CLEARED on SEC-1 was a one-word
residual (Edge Cases still said "mechanical"); fixed at `spec.md` Edge Cases "Secret leakage" bullet,
unanimous direction — cycle closes without a second loop.

## Held 🟡 (recorded, non-gating — operator proceeds at will)

- **TOC-2 / TOC-3** (Goldratt) — **the load-bearing strategic caveat.** Spec 004's read-side has
  never been exercised by a real run, and **004 is itself still `Draft`** (Richards confirmed). The
  write-side is being built on an unratified, never-round-tripped schema. Recommended cheapest
  experiment before relying on the automated phase: run **one** full lifecycle, persist write-back
  **by hand** into 2–3 lens records, confirm run 2's exploratory phase reads them back. Did not gate
  (most voters CONFIRMed; the cost-of-delay rationale is recorded and legible) but the operator owns
  this call.
- **ARCH-3** (Richards) — add a doc-test fitness function asserting the FR-010 section names no
  fingerprint write-verb, so the 004 reuse can't silently fork. Cheap hardening, non-blocking.

## Demoted to 🟢 by the tally (net −2)

- **SEC-6** — memory dir is outside the repo; a repo `.gitignore` was never its control surface.
- **DDD-2** — "write-back" judged to name the trust-bearing direction more honestly than "proposal."
- **COOP-3** — out-of-band re-presentation cuts against the deliberate sign-off-bookend design; the
  bounded-lapse fix (COOP-2) already removes the fatigue tax it worried about.

## S1–S9 self-audit

| Inv | Status | Evidence |
|-----|--------|----------|
| S1 | **deviation (operator-directed)** | Orchestrator authored canon edits directly, not via speckit phase-runner; flagged above. Vote-honesty invariants held. |
| S2 | n/a | Single gate; RSVP fired once, independently. |
| S3 | pass | Panel = 5; overflow seated by two-axis signal; tie-at-cap = 🟡 recorded default (`gateC-seating-1`), no operator interrupt, no lens-merit judgment. |
| S4 | pass | No 🔴 passed open; all 3 resolved **and verified** by re-vote. |
| S5 | **deviation (operator-directed)** | Incorporation was direct canon editing, not speckit-regenerated; flagged above. |
| S6 | pass | Every counted finding carried `file:line` or a `[principle]` tag; none demoted to `[unsupported]`. |
| S7 | pass | 1 self-heal cycle, cleared; bound (3) not approached. |
| S8 | pass | No persona voted on its own finding (Security skipped SEC-*, etc.). |
| S9 | pass | All votes were real dispatches; stage-4 severity is arithmetic over them, not orchestrator opinion. |

## Memory update (sign-off)

n/a — this run was a **single Gate C**, not a full lifecycle reaching sign-off, so the 010 phase
(which this feature *specifies*) did not itself fire. No per-persona write-back was dispatched; no
addendum proposal collated.
