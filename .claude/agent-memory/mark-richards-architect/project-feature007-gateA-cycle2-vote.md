---
name: project-feature007-gateA-cycle2-vote
description: Feature 007 Gate A CYCLE 2 cross-vote — 19 PRIORITIZE / 2 OVER-RATE; plugin-template red converged (3 lenses); C6 regex held yellow vs dno/unclebob red
metadata:
  type: project
---

Feature 007 Gate A cycle-2 cross-vote (2026-06-10). Voted 19 P / 2 O on 20 findings
(cooper G1-G5, dno G6-G11, norman G18-G22, unclebob G23-G27). All runtime re-verified.

**Convergences (independent rediscovery of my authored findings):**
- G6 (dno RED, plugin template never delivered to plugin channel) = my authored red
  exactly; ls skill/chorus-review/ has no templates/, plugin.json ships skill+agents
  only, R5 claims "the plugin's skill directory". PRIORITIZED. Family F residue is the
  cycle's consensus red.
- C6 dead regex found by 3 lenses (me yellow, dno G7 + unclebob G23 red). I OVER-RATED
  their reds: SC-008 stands on three legs (C4 pins verified non-vacuous, C7 behavioral
  matrix, C6 scan); C6's Write/Edit + redirection branches DO fire (empirically: 2/5
  test lines hit); manual backstop named at quickstart.md:139. One-line fix = yellow.
  Cycle-1 precedent: C3's dead regex was also yellow-banded. Consistency held.
- C5 dead stanza ($CLAUDE_HOME_TMP_CHECK) found by 4 lenses — I had it as a nit;
  cooper/dno/unclebob banded yellow. I PRIORITIZED the yellows: dead *effectful* code
  (duplicate install + leaked tempdir per run) in the conformance layer the revision
  leans on is should-address, not note. Calibration update: check-layer hygiene
  defects rate one band higher than I instinctively give them when the checks ARE the
  design's mechanical defense.

**Strong findings I did not have (adopted):**
- G1/G18 (cooper/norman): the recovery affordances are addressed to the author's
  machine — R10's "point to the repo path" + FR-006's "clone + ./install.sh" remedy
  are single-channel while detection is dual-channel. Same family-F coordinate-system
  disease as G6, on the instruct/recovery surfaces.
- G26 (unclebob): C2/C3 pass vacuously on zero Cites: lines (while-loop over absent
  matches emits nothing) — cardinality assert missing. Same vacuity class as C6.
- G9 (dno): no artifact names who runs C1-C7 or when — unowned fitness functions are
  decorative; fix is one ledger sentence. Squarely my doctrine; I missed it.
- G19 (norman): navigation.md:16 pins the bare "Exit the tutorial" label while the
  cheat-sheet lives in the behavior column — family A's resolution invisible at
  selection time. Contract-vs-walkthrough label divergence.
- G24 (unclebob): walkthrough S5 shows "Recap this step" with no S5 deeper pass —
  contradicts navigation.md:14's per-step depth scoping.

**How to apply:** at cycle 3 / Gate B, check: templates joins plugin packaging or
resolution rule names plugin-root fallback; C6 regex fixed (\| → |); C5 stanza
deleted; C2 cardinality assert; recovery pointers/remedies branch on channel;
C1-C7 run-point named in plan or ledger. Pattern worth keeping: the "dead check"
disease (C3 cycle-1, C5/C6/C2-C3-vacuity cycle-2) recurs every time checks are
regenerated — checks need their own empirical probe recorded next to them.
