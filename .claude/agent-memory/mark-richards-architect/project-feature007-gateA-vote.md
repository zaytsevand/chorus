---
name: project-feature007-gateA-vote
description: Feature 007 Gate A cross-vote — 48 PRIORITIZE / 3 OVER-RATE (F15 S5-overflow absorbed by Other; F23 §-drift is green per SC-005's doc-axis wording; F25 plan mislabel bounded by correct contract+R8); SC-003 cluster elevated to red on peers' sharper articulation
metadata:
  type: project
---

Feature 007 Gate A cross-vote (2026-06-10), arch lens grading Norman/UncleBob/Evans/Cooper
(51 findings). Outcome: 48 PRIORITIZE, 3 OVER-RATE.

**Why the three OVER-RATEs:**
- F15 (S5 jump overflow, proposed red): real but yellow — built-in Other "needs no slot;
  arbitrary asks are answered, then the question is re-presented" (navigation.md:17-18),
  and from S5 the follow-up lists S1–S4 = exactly 4; "back" duplicates Other's default.
  Matches my own register (I filed it yellow).
- F23 (§-anchor drift, proposed yellow): SC-005's literal promise is the doc axis
  ("a renamed doc", spec.md:216-218) and data-model.md:85-87 honestly scopes validation
  to doc-resolution; §-drift lands in the right file = green (Evans F37's framing).
- F25 (plan "three-mode-aware SKILL.md from 006", proposed yellow): mislabel real
  (installed 006-era SKILL.md still "## Two modes") but bounded — learn-mode.md:11
  correctly pins the registration edit, and the merge-conflict expectation stays valid
  because 006 does insert decision text in the same region (my runtime check, [[project-feature007-gateA]]).

**Calibration adopted from peers:** the SC-003 cluster (F2/F50 red) I had filed yellow —
Norman/Cooper's sharper articulation (US2's independent test is literally unsatisfiable
by the S1 slot table; the quickstart fudges its own metric at :54-55; N2's fast-exit
disjunct factually false) convinced me red is right: acceptance criteria are the topmost
contract. Also adopted unclebob's F16 at red: SKILL.md:3 deliberately carries both
existing trigger phrases in the frontmatter description (the harness routing surface),
yet learn-mode.md:8-13 + FR-013 + C1 all miss it — entry-point seam unpinned.

**New verifications this round (beyond [[project-feature007-gateA]]):**
- SKILL.md:488-490 Phase-5 addendum offer = second writer to docs/reviews/CHORUS-PROJECT.md
  (F22 true); SKILL.md:66-69/112-115 consumer contract is binary present/absent, so the
  scaffold's TO-FILL state is contractually undefined (F33 true; README:279 "incomplete"
  has no canon backing).
- C4 comment promises an RSVP-quorum grep that doesn't exist; R7's fourth block is the
  four-stage list, not the quorum table (quickstart.md:90-97 vs research.md:117-119).
- FR-010 waives "across machines" while the assumption waives "sessions or machines" —
  internal spec discrepancy (F52 true).
- Dogfood walkthrough is mandated in-repo and the illustrated path accepts the scaffold
  (plan testing §, quickstart:25) — accepting leaves this repo a behavior-changing
  addendum (F61 true; decide deliberately).

**How to apply:** at cycle 2 / Gate B, check spec amended FR-004+SC-003+US2, the
registration contract gained the frontmatter-description row, C3 got an allowlist or
cite syntax, C6 got a write-idiom scan, and the S2 confirm fork was struck
(scaffold.md:26). Re-raise any still open.
