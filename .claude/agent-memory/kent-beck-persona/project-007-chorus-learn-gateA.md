---
name: project-007-chorus-learn-gateA
description: Gate A run 2 (clean-slate) exploratory record for feature 007 chorus learn — feedback-loop map, what the C-suite corners vs what only dogfood sees
metadata:
  type: project
---

Gate A run 2 (clean-slate, seated 2026-06-12), feature 007 `chorus learn` (chorus-review repo, design gate — markdown only).

**Feedback-loop map (my gate, answered)**: structural surfaces are pinned by C1–C7+C5b in quickstart.md, owned and triggered at the Gate C dogfood + pre-merge, results to the gate ledger (SC-008). Conversational behavior (navigation N1–N4, pinned labels, per-step depth state, ResumeState) has NO mechanical check — verified only by the manual Gate C dogfood walkthrough. That asymmetry is the load-bearing fact for any finding I author.

**Cornering**: scaffold is corner-able (C7 scratch-repo matrix, all 4 paths: accept/decline/existing/outside-repo). AskUserQuestion flows resist static cornering — that resistance is the finding-shaped thing, not a defect per se.

**Clean-slate wrinkle**: run 1's ledger (agent-sdlc-log.md) ended BLOCKED with β-cluster "checks don't assert their invariant" (proposed C8 nav-label check; C6 one-directional self-test; C3 total-not-per-step floor). Current corpus (cycle-3 regen) still lacks C8 — I verified directly. Open operator question emitted: is run-1's log corpus or context?

**Why:** run 2 findings must trace to the current artifacts, not run 1's verdicts.
**How to apply:** at finding/vote stages of this gate, cite quickstart/contracts spans directly; treat run-1 ledger as provenance only unless the operator rules otherwise.

No ratified constitution (unfilled template since feature 003); de-facto principle "one canonical definition, layers cite never restate" is the local TDD-analog discipline.
